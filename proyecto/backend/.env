# SMTP configuration for forgot password emails
# Replace these values with your SMTP provider credentials
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=novatech69xd@gmail.com
SMTP_PASS=hifhohddxtbeexls
# Optional: custom From header. Defaults to SMTP_USER if not set
SMTP_FROM="NovaTech <no-reply@novaTech.com>"
